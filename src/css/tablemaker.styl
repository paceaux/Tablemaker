#tablemaker {
  width: 400px;
  height: 300px;
  border: 2px solid #333;
}

#editor {
  position: fixed;
  right: 0;
  top: 0;
  border-radius: 5px;
  border: 1px solid black;
  box-shadow: -2px 2px 4px rgba(150, 150, 150, .5);
}

fieldset {
  margin: 1em 2em;
  width: 100%;
  border: 1px solid rgb(150, 150, 150);
  border: 1px solid rgba(150, 150, 150, 5);
  padding: .5em;
  border-radius: 5px;
}

.controls {
  width: 180px;
  margin: 1em 1em 1em 2em;
  float: left;
}

label {
  display: block;
}

[type="range"] {
  width: 90%;
  display: inline;
}

[type="range"]:after {
  display: inline;
  float: left;
  content: attr(data-value)'/' attr(max);
}

span.custom {
  display: none;
}

#tableHTML {
  height: 250px;
  display: none;
  border-radius: 10px;
  padding: 1em;
  font-family: monospace;
  box-shadow: 2px 4px 3px #999, -2px 4px 3px #999;
  background: #fff;
  z-index: 9999;
  position: fixed;
  right: 50%;
  top: 0;
}

.tableCode {
  height: 90%;
  width: 90%;
}

.closeCode {
  display: block;
  float: right;
  padding: .5em;
  border-radius: 50%;
  box-shadow: 1px 1px 2px rgba(100, 100, 100, .7);
  position: relative;
  top: -1.75em;
  right: -1em;
}

#tablemaker table {
  outline: 3px solid #999;
  width: 200px;
}

tr {
  outline: 1px solid #888;
  height: 20px;
  width: 100%;
  background: rgba(150, 150, 150, .8);
}

tr:nth-child(even),
.tr--even{
  background: rgba(150, 150, 150, .6);
}


tr:nth-child(odd), 
.tr--odd{
  background: rgba(150, 150, 150, .7);
}

tr[class*="nth"] {
  background: rgba(100, 150, 150, .7);
}

td {
  outline: 1px solid #555;
  background: rgba(240, 240, 240, .3);
  border: 2px solid #555;
}

td:nth-child(even) {
  background: rgba(240, 240, 240, .7);
}

td.even {
  background: rgba(240, 200, 240, .7);
}

td.odd {
  background: rgba(240, 200, 240, .7);
}

td[class*="cust"] {
  background: rgba(150, 150, 100, .7);
}










td.even {
  background: rgba(30, 80, 30, .3);
}

td.odd {
  background: rgba(30, 80, 30, .7);
}

.custom {
  border: none;
  outline: none;
  text-align: right;
  font-size: 1.1em;
}

.rowAdjustContainer {
  position: fixed;
  display: none;
  font-size: .8em;
  box-shadow: 2px 4px 3px #999, -2px 4px 3px #999;
  background: #fff;
  z-index: 9999;
}

#rowAdjuster {
  width: 75px;
}
